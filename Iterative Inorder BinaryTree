public class InorderTraversal{
 class TreeNode{
    int val;
    TreeNode left,right;
    }
pubic List<Integer> solution(TreeNode root){
  Stack<TreeNode> s=new Stack<>();
  List<Inteegr> output=new List<>();
  if(root==null)
    return output;
  TreeNode cur=root;
  while(cur!=null || !s.isEmpty()){
    while(cur!=null){
      stack.push(cur);
      cur=cur.left;   //Move to Left Node
     }
    cur=stack.pop();
    output.add(cur.val);    //Print Node , i.e. add to output list
    cur=cur.right;      //Move to Right Node
   }
  reutn output;
 }
}
